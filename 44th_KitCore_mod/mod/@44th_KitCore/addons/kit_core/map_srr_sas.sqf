// SRR & SAS legacy class -> role key.
//
// Intentionally empty. The ORBAT composition (now canonical) kits SRR and SAS
// slots directly via each unit's FTH_kit attribute, using the exact crate kit
// names, so no class mapping is needed there. This file only matters for the
// superseded browser-placed _44th_SRR_* / _44th_SAS_* classes, whose names do
// not line up 1:1 with kits; add entries here only if those classes are revived.
